namespace RBLclass.Outlook.Adapter
{
    /// <summary>
    /// Placeholder marker for the Phase 1 skeleton (Step 0). The Outlook
    /// adapter has no live COM code yet - the IMailStore / IFolderTree
    /// implementations land in Step 1 (folder-tree walk over the OM with
    /// ComRef&lt;T&gt;). Keeps the project non-empty and gives those types a home.
    /// </summary>
    internal static class AdapterInfo
    {
    }
}
