<?xml version="1.0" encoding="UTF-8"?>
<customUI xmlns="http://schemas.microsoft.com/office/2009/07/customui">
  <ribbon>
    <tabs>
      <tab id="RBLclassTab" label="RBLclass">
        <group id="RBLclassGroup" label="RBLclass">
          <button id="TogglePaneButton"
                  label="RBLclass pane"
                  size="large"
                  imageMso="MoveToFolder"
                  onAction="OnTogglePaneClick"
                  screentip="Show or hide the RBLclass pane"
                  supertip="Open the RBLclass pane: search the folder tree, jump to a folder (open button), and file the selected mail - single-click a folder to add it as a destination, double-click to file into it." />
          <button id="AutoClassButton"
                  label="Auto-class"
                  size="large"
                  imageMso="CategorizeMenu"
                  onAction="OnAutoClassClick"
                  screentip="Auto-classify the selected mail(s)"
                  supertip="File each selected mail to the folder(s) its conversation was last classified into. Mails whose conversation was never classified are left alone. Tip: right-click this button and choose 'Add to Quick Access Toolbar' for one-click filing." />
          <button id="RemoveAttachmentsButton"
                  label="Remove attachments"
                  size="large"
                  imageMso="AttachmentRemove"
                  onAction="OnRemoveAttachmentsClick"
                  screentip="Remove attachments"
                  supertip="Strip all attachments from the selected mail(s), after confirmation." />
          <button id="RefreshFoldersButton"
                  label="Refresh folders"
                  size="large"
                  imageMso="Refresh"
                  onAction="OnRefreshFoldersClick"
                  screentip="Refresh the folder index"
                  supertip="Re-scan your PST stores so folders you created or renamed directly in Outlook show up in search. Run this after adding folders by hand." />
          <button id="IndexStatusButton"
                  label="Index status"
                  size="large"
                  imageMso="Info"
                  onAction="OnIndexStatusClick"
                  screentip="Folder index status"
                  supertip="Shows whether the folder index was loaded from cache or freshly walked, with store/folder counts. Step 1 diagnostic - the search UI arrives in Step 3." />
          <button id="SettingsButton"
                  label="Settings"
                  size="large"
                  imageMso="ToolsOptions"
                  onAction="OnSettingsClick"
                  screentip="RBLclass settings"
                  supertip="Configure folder search, classify, sending guards and sent-item triage. Every change applies and persists immediately." />
        </group>
      </tab>
    </tabs>
  </ribbon>
</customUI>
