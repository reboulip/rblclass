<UserControl x:Class="RBLclass.AddIn.Views.FolderSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:v="clr-namespace:RBLclass.AddIn.Views"
             d:DesignHeight="400" d:DesignWidth="320">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" x:Name="QueryBox" Margin="0,0,0,6"
                 Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                 KeyDown="QueryBox_KeyDown" />

        <CheckBox Grid.Row="1" Margin="0,0,0,6" Content="All results"
                  IsChecked="{Binding AllResults}" />

        <ListBox Grid.Row="2" x:Name="ResultsList"
                 ItemsSource="{Binding Results}"
                 SelectedItem="{Binding SelectedResult}"
                 MouseDoubleClick="ResultsList_MouseDoubleClick"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- Show the END of long paths (leading ellipsis) so the leaf
                         stays visible in the narrow pane; full path on hover. -->
                    <TextBlock v:PathTrim.FullPath="{Binding DisplayPath}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="3" Margin="0,6,0,6" Style="{DynamicResource MutedText}"
                   Text="{Binding Status}" TextWrapping="Wrap" />

        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <Button Content="Open" Click="Open_Click" Margin="0,0,8,0" Padding="12,3" />
            <CheckBox Content="Open in a new window" VerticalAlignment="Center"
                      IsChecked="{Binding OpenInNewWindow}" />
        </StackPanel>
    </Grid>
</UserControl>
