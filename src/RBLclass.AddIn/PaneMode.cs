namespace RBLclass.AddIn
{
    /// <summary>Which view the single RBLclass task pane is currently showing.</summary>
    public enum PaneMode
    {
        OpenFolder = 0,
        Classify = 1
    }
}
