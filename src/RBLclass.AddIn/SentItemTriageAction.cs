namespace RBLclass.AddIn
{
    /// <summary>The choice offered by the sent-item triage prompt (legacy 6c, reworked).</summary>
    public enum SentItemTriageAction
    {
        MoveToInbox = 0,
        Delete = 1,
        Leave = 2
    }
}
