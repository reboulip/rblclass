namespace RBLclass.AddIn
{
    /// <summary>The choice offered by the sent-item triage prompt (legacy 6c).</summary>
    public enum SentItemTriageAction
    {
        Class = 0,
        Delete = 1,
        MoveToInbox = 2,
        Leave = 3
    }
}
