namespace RBLclass.Core
{
    /// <summary>
    /// Minimal product-identity marker. Placeholder content for the Phase 1
    /// solution skeleton (Step 0) so the core assembly has a referenceable,
    /// testable type before the real domain interfaces land. Safe to keep or
    /// replace as the folder-tree model arrives in Step 1.
    /// </summary>
    public static class ProductInfo
    {
        /// <summary>Product name, shared across layers.</summary>
        public const string Name = "RBLclass";
    }
}
